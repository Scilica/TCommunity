{# src/OC/PlatformBundle/Resources/views/guide/index.html.twig #}

{% extends "TCMainBundle::layout.html.twig" %}

{% block title %} {{ parent() }} - guide {% endblock %}

{% block body %}

	{% for guide in monguide %}
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <div class="row">
        <div class="row">
          <div class="col-md-12 col-lg-8 imglol">
            <img src="{{ guide.miniatureUrl }} " alt="miniature" class="img-responsive imgcadre" />
        </div>
        <div class="hidden-md hidden-sm hidden-xs col-lg-4 img456">
          <div class="row">
            <div class="content1">
              <div class="heading_widget col-lg-12">
                Fiche du jeu
              </div>
              <div class="game_info col-lg-12">
                <div class="row">
                  <div class="col-lg-5 s1">
                    <img src="{{ guide.game.jaquetteUrl }} " alt="Jaquette face final fantasy xiv" class="img-responsive test123 divimg"  />
                  </div>
                  <div class="col-lg-7 tablo">
                    <div class="row">
                      <div class="col-lg-12 cell">
                        <h1>{{guide.game.title}}</h1>
                      </div>
                      <div class="col-lg-12 cell">
                        <span class="label1">Editeur(s):</span> <span class="h21">{{guide.game.editor}}</span>
                      </div>
                      <div class="col-lg-12 cell">
                        <span class="label1">Plateforme(s):</span> <span class="h21">{{guide.game.plateform}}</span>
                      </div>
                      <div class="col-lg-12 cell">
                        <span class="label1">Genre(s):</span> <span class="h21">{{guide.game.type}}</span>
                      </div>
                      <div class="col-lg-12 cell">
                        <span class="label1">Sortie France:</span> <span class="h21">{{guide.game.date|date("d/m/Y")}}</span>
                      </div>  
                    </div>
                  </div> 
                </div>
                <div class="row">
                  <div class="col-lg-12">
                    <button class="btn gameindex_button_blue" onclick="window.location.href='{{ path('tc_writer_jeux_view', {'jeux': guide.game.title| replace({' ': '_'})}) }}'" >Voir la fiche du jeu</button>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <div class="row">
            <div class="content2">
              <div class="redactor_info col-lg-12">
                <div class="row">
                  <div class="col-lg-4">
                    <img src="/Symfony/web/gallery/image/Avatar/avatar.png" alt="" class="img-responsive userimg" />
                  </div>
                  <div class="col-lg-8 tablo2">
                    <div class="row">
                      <div class="col-lg-12 cell2">
                        <span class="label2">Rédigé par </span> <span class="h23">{{guide.author}}</span>
                      </div>
                      <div class="col-lg-12 cell2">
                        <span class="label2">Publié le </span><span class="h24">{{guide.date|date("d/m/Y")}}</span>
                      </div>
                    </div>
                  </div> 
                </div>
              </div>
            </div>
          </div>      
        </div>
    <div class="col-md-12 col-lg-12">
          <div class="title_content">{{ guide.title }}</div>
          <div class="content">
            {{ guide.content|raw}}
          </div>
          <div class="tag">
            {%for tag in guide.tag %}
            <span><a class="btn tag_button {{ tag.color }}" href="{{ path('tc_writer_tag_view', {'tag': tag.title| replace({' ': '_'})}) }}">{{ tag.title }}</a></span>
            {% endfor %}
          </div>
          {% endfor %}
    </div>
    <div class="col-md-12 col-lg-12">
      <div class="row">
        <div class="heading_big_picture col-md-12 col-lg-12">A voir aussi</div>
      </div>
{% for guides in otherguides %}
        <div class="div col-xs-12 col-sm-12 col-md-4 col-lg-4 ">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
              <div class="row">
              <div class="encart_small"><h1>guides</h1></div>
                <img src=" {{ guides.miniatureUrl }} " alt="{{ guides.miniatureAlt }}" class="img-responsive dididov" onclick="window.location.href='{{ path('tc_writer_guides_view', {'jeux': guides.game.title| replace({' ': '_'}) ,'category': guides.category.title| replace({' ': '_'})| replace({'\'': '-'}) ,'id': guides.id }) }}'" />
            <div class="title" onclick="window.location.href='{{ path('tc_writer_guides_view', {'jeux': guides.game.title| replace({' ': '_'}) ,'category': guides.category.title| replace({' ': '_'})| replace({'\'': '-'}) ,'id': guides.id }) }}'">
              <h1>{{ guides.title }}</h1>
            </div>
        </div>
      </div>
      </div>
      {% endfor %}
    </div>
  </div>

{% endblock %}